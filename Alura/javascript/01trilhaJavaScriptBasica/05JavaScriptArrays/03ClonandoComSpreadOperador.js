const notas = [7, 7, 8, 9];

const novasNotas = [...notas, 10];
//tambem podemos adicionar com uma virgula no codigo acima
//novasNotas.push(10);

console.log(`As notas antigas são ${notas}`);
console.log(`As novas notas são ${novasNotas}`);